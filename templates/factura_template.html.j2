<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #2c3e50;
            background: white;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 40px;
            background: white;
        }
        
        /* HEADER */
        .header {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: white;
            padding: 30px;
            margin: -40px -40px 40px -40px;
            text-align: center;
        }
        
        .header h1 {
            font-size: 36px;
            margin-bottom: 5px;
            color: #ff6b6b;
        }
        
        .header .number {
            font-size: 14px;
            opacity: 0.9;
        }
        
        /* BADGE */
        .badge {
            display: inline-block;
            background: #ff6b6b;
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 11px;
            font-weight: bold;
            margin-bottom: 20px;
        }
        
        /* SECTIONS */
        .section {
            margin-bottom: 30px;
        }
        
        .section-title {
            font-size: 13px;
            color: #ff6b6b;
            text-transform: uppercase;
            font-weight: bold;
            letter-spacing: 1px;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 2px solid #ff6b6b;
        }
        
        /* INFO BOXES */
        .info-box {
            background: #fff5f5;
            padding: 15px;
            margin-bottom: 15px;
            border-left: 4px solid #ff6b6b;
            border-radius: 4px;
        }
        
        .info-box p {
            margin: 5px 0;
            font-size: 13px;
        }
        
        .info-box strong {
            color: #1a1a2e;
            display: inline-block;
            width: 120px;
        }
        
        /* TABLE */
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        
        thead {
            background: #1a1a2e;
            color: white;
        }
        
        th {
            padding: 12px;
            text-align: left;
            font-weight: bold;
            font-size: 12px;
            text-transform: uppercase;
        }
        
        td {
            padding: 12px;
            border-bottom: 1px solid #ecf0f1;
            font-size: 13px;
        }
        
        th.align-right,
        td.align-right {
            text-align: right;
        }
        
        tbody tr:hover {
            background: #fff5f5;
        }
        
        /* TOTALS BOX */
        .totals {
            float: right;
            width: 350px;
            background: #fff5f5;
            border: 2px solid #ff6b6b;
            border-radius: 4px;
            padding: 20px;
            margin-top: 20px;
        }
        
        .total-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid #ddd;
            font-size: 13px;
        }
        
        .total-row.final {
            background: #1a1a2e;
            color: white;
            padding: 15px;
            margin: 15px -20px -20px -20px;
            border: none;
            border-radius: 0 0 2px 2px;
            font-weight: bold;
            font-size: 18px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .total-row strong {
            font-weight: bold;
        }
        
        .total-row:last-child {
            border-bottom: none;
        }
        
        /* PAYMENT TERMS */
        .payment-terms {
            clear: both;
            background: #fff3cd;
            border-left: 4px solid #ffc107;
            padding: 15px;
            margin: 30px 0;
            border-radius: 4px;
        }
        
        .payment-terms h3 {
            font-size: 13px;
            color: #856404;
            margin-bottom: 10px;
        }
        
        .payment-terms p {
            font-size: 12px;
            color: #856404;
            margin: 5px 0;
        }
        
        /* FOOTER */
        .footer {
            margin-top: 40px;
            padding-top: 20px;
            border-top: 2px solid #ecf0f1;
            text-align: center;
            font-size: 10px;
            color: #7f8c8d;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- HEADER -->
        <div class="header">
            <h1>FACTURA</h1>
            <div class="number">#{{ factura_numero }} | {{ fecha }}</div>
        </div>
        
        <div class="badge">⚖️ DOCUMENTO LEGAL - ORIGINAL</div>
        
        <!-- DATOS EMPRESA Y CLIENTE -->
        <div class="section">
            <div class="section-title">Datos Fiscales de la Empresa</div>
            <div class="info-box">
                <p><strong>Empresa:</strong> Pintura Express S.L.</p>
                <p><strong>CIF:</strong> A12345678</p>
                <p><strong>Dirección:</strong> Calle Principal, 123 | 28001 Madrid</p>
                <p><strong>Teléfono:</strong> +34 912 345 678</p>
                <p><strong>Email:</strong> facturacion@pinturaexpress.com</p>
            </div>
        </div>
        
        <div class="section">
            <div class="section-title">Datos del Cliente</div>
            <div class="info-box">
                <p><strong>Nombre:</strong> {{ cliente_nombre }}</p>
                <p><strong>NIF/CIF:</strong> {{ cliente_nif }}</p>
                <p><strong>Dirección:</strong> {{ cliente_direccion }}</p>
                <p><strong>Email:</strong> {{ cliente_email }}</p>
            </div>
        </div>
        
        <!-- TABLA DE SERVICIOS -->
        <div class="section">
            <div class="section-title">Servicios Prestados</div>
            <table>
                <thead>
                    <tr>
                        <th>Concepto</th>
                        <th class="align-right">Cantidad</th>
                        <th class="align-right">Precio Unitario</th>
                        <th class="align-right">Importe</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in items %}
                    <tr>
                        <td><strong>{{ item.concepto }}</strong></td>
                        <td class="align-right">{{ item.cantidad }}</td>
                        <td class="align-right">{{ item.precio_unitario }}</td>
                        <td class="align-right"><strong>{{ item.importe }}</strong></td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        
        <!-- TOTALES -->
        <div class="totals">
            <div class="total-row">
                <span>Subtotal:</span>
                <span>{{ subtotal }} €</span>
            </div>
            <div class="total-row">
                <span>IVA (21%):</span>
                <span>{{ iva }} €</span>
            </div>
            <div class="total-row final">
                <span>TOTAL FACTURA:</span>
                <span>{{ total }} €</span>
            </div>
        </div>
        
        <!-- CONDICIONES DE PAGO -->
        <div class="payment-terms">
            <h3>💳 Condiciones de Pago</h3>
            <p><strong>Plazo:</strong> 30 días desde la fecha de emisión</p>
            <p><strong>Forma de pago:</strong> Transferencia bancaria</p>
            <p><strong>Banco:</strong> ES12 1234 5678 90 1234567890</p>
            <p><strong>Concepto:</strong> Factura {{ factura_numero }}</p>
        </div>
        
        <!-- FOOTER -->
        <div class="footer">
            <p>Factura generada automáticamente | Válida sin firma según normativa vigente</p>
            <p>Pintura Express S.L. | CIF: A12345678 | Registro Mercantil de Madrid</p>
        </div>
    </div>
</body>
</html>